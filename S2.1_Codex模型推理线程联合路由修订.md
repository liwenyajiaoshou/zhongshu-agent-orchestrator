# 中枢 S2.1｜Codex 模型—推理—线程联合路由修订 V1.0

日期：2026-08-27

## 1. 本次范围

本次升级**只调整 Codex 线程**。

不调整：
- 网页版 ChatGPT 对话线程的模型切换；
- 对话线程的模型绑定；
- 对话线程的上下文策略。

## 2. 解决的问题

旧规则容易产生：

```text
当前 Codex 线程
→ 直接把模型从较低档切到较高档
→ 继续施工
```

这会忽略两个关键变量：

1. 当前 Codex 上下文是否仍然高价值；
2. 当前问题究竟是“推理预算不足”，还是“模型能力不足”。

## 3. 新规则

中枢以后联合判断：

```text
Codex 上下文质量
+
当前模型能力
+
当前推理等级
```

### 情况 A｜模型够，上下文强关联

```text
Codex 线程：继续
模型：保持
推理等级：提高
```

### 情况 B｜模型能力不足

如果已有实质施工上下文：

```text
Codex 线程：更换
模型：升级
交接：压缩权威状态
```

### 情况 C｜上下文污染

```text
Codex 线程：更换
模型：按当前任务重新选择
```

即使模型不变，也可以换线程。

### 情况 D｜模型降级

复杂分析完成后若后续任务变成机械施工：

```text
高档模型 Codex 线程
→ 压缩交接
→ 新的较低档模型 Codex 线程
```

不在原长线程中直接降级。

## 4. 唯一例外

原 Codex 线程可以直接换模型，仅限：

- 刚创建；
- 尚未实质施工；
- 没有重要中间判断；
- 工具调用很少；
- 没有高价值上下文需要保留。

## 5. 长期原则

> **Codex 线程默认模型稳定；任务变复杂时先判断是否只需要提高推理等级。模型升级是能力不足后的动作，不是复杂度上升后的默认动作。**

> **已有实质施工上下文时，Codex 模型变化默认意味着通过压缩交接进入新的 Codex 线程。**
